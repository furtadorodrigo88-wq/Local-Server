# schemas/categoria.yaml

components:
  schemas:
    Categoria:
      type: object
      properties:
        id:
          type: string
          description: ID da categoria
        nome:
          type: string
          description: Nome da categoria
        descricao:
          type: string
          description: Descrição da categoria
        enabled:
          type: boolean
          description: Status da categoria
        created_at:
          type: string
          format: date-time
          description: Data de criação da categoria
        updated_at:
          type: string
          format: date-time
          description: Data de atualização da categoria
    CategoriaCreate:
      type: object
      properties:
        designacao:
          type: string
          description: Designação da categoria
        icone:
          type: string
          description: Icone da categoria
    ResponseSucessoCategoria:
      type: object
      properties:
        message:
          type: string
          example: "Categoria criada com sucesso"
        status:
          type: boolean
          example: true
        data:
          $ref: "#/components/schemas/Categoria"
    ResponseErroCategoria:
      type: object
      properties:
        message:
          type: string
          example: "Erro ao processar categoria"
        status:
          type: boolean
          example: false
        data:
          type: object
          nullable: true